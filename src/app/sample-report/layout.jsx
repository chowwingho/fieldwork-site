export const metadata = {
  title: "Sample Report",
  description: "See what you'll get after running the AI Readiness Assessment with your team.",
};

export default function Layout({ children }) {
  return children;
}
